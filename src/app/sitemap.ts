import type { MetadataRoute } from "next";
import { getBrands, getCategories, getProducts, getShops } from "@/lib/catalog";
export default async function sitemap():Promise<MetadataRoute.Sitemap>{const base=process.env.NEXT_PUBLIC_APP_URL||"http://localhost:3000";const[products,categories,brands,shops]=await Promise.all([getProducts({limit:500}),getCategories(),getBrands(),getShops()]);return[{url:base,changeFrequency:"daily",priority:1},{url:`${base}/products`,changeFrequency:"daily",priority:.9},{url:`${base}/categories`,changeFrequency:"weekly",priority:.8},...products.map(p=>({url:`${base}/product/${encodeURIComponent(p.slug)}`,changeFrequency:"weekly" as const,priority:.7})),...categories.map(c=>({url:`${base}/category/${encodeURIComponent(c.slug)}`,changeFrequency:"weekly" as const,priority:.6})),...brands.map(b=>({url:`${base}/brand/${encodeURIComponent(b.slug)}`,changeFrequency:"weekly" as const,priority:.5})),...shops.map(s=>({url:`${base}/shop/${encodeURIComponent(s.slug)}`,changeFrequency:"weekly" as const,priority:.5}))]}
